Compare bb version of building count output with output of 
old version JOINED with the grid and filtered to the bbox.
Results should be the same - bb version should be faster.

Note that non-bb version of Moscow query is likely to be 
very slow indeed (hours)
